import 'package:flutter/material.dart';


void main() => runApp(Mysignup());

class Mysignup extends StatelessWidget {
  const Mysignup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
